/** Releases tagged Pet, plus those that apply to both apps. */
export const SYSTEM_RELEASE_PRODUCTS_PET = ['pet', 'all'] as const;

export const CHANGELOG_PAGE_SIZE = 8;

/** Body longer than this shows "Leer más" */
export const CHANGELOG_BODY_COLLAPSE_CHARS = 480;
